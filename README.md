# File Management Exam

For first time running the app can run these commands

- pnpm install
- pnpm dev
- or you can run both in one go `js pnpm install && pnpm dev`

For the second time onwards

- can just run `js pnpm dev` command
